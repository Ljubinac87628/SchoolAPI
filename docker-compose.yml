version: '3.8'

services:
 schoolapi:
  build:
   context: .
   dockerfile: Dockerfile
  container_name: schoolapi_container
  ports:
  - "5000:80"
  environment:
   - ASPNETCORE_ENVIRONMENT=Development